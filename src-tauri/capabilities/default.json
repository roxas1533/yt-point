{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all windows",
  "windows": ["main", "viewer"],
  "permissions": [
    "core:default",
    "shell:default",
    "core:event:default"
  ]
}
